Place your first-run splash GIF here as:

  loading_bg.gif

RainTux looks for this file automatically (one full animation loop, then the terms dialog).

You can override with RAINTUX_SPLASH_GIF or use raintux/data/assets/splash.gif instead.
